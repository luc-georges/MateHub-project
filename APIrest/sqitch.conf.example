# Voir le readme pour initialisation de sqitch

[core]
	engine = pg
	top_dir = migrations
	# plan_file = migrations/sqitch.plan
# [engine "pg"]
	target = db:pg://login:password@localhost:5432/database
	# registry = sqitch
	# client = psql
